extern crate section17_release;
use section17_release::mix;
use section17_release::PrimaryColor;
fn main() {
    let red = PrimaryColor::Red;
    let yellow = PrimaryColor::Yellow;
    mix(red, yellow);
    s
}
